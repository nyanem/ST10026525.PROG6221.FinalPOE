<Window x:Class="ST10026525.PROG6221.POE.RecipeDisplay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ST10026525.PROG6221.POE"
        mc:Ignorable="d"
       Title="Selected Recipe" Height="450" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontWeight="Bold" FontSize="16" Text="{Binding RecipeName}"/>

        <TextBlock Grid.Row="1" Margin="0,10,0,5" Text="Ingredients:"/>
        <ListBox Grid.Row="2" ItemsSource="{Binding Ingredients}" Margin="0,0,0,10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding DisplayString}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock Grid.Row="3" Margin="0,10,0,5" Text="Instructions:"/>
        <TextBox Grid.Row="4" Text="{Binding Instructions}" IsReadOnly="True" TextWrapping="Wrap"/>
    </Grid>
</Window>
